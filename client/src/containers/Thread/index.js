import { connect } from 'react-redux';

import Thread from 'src/components/Forum/Threads/Thread';

const mapStateToProps = () => ({
});

const mapDispatchToProps = {};


export default connect(mapStateToProps, mapDispatchToProps)(Thread);
